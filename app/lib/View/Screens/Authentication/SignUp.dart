import 'package:flutter/material.dart';

class SignUpScreen extends StatelessWidget {
  // Your code for the sign-in screen goes here
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // Your sign-in UI here
    );
  }
}
