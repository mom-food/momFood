import 'package:flutter/material.dart';

class SuccessCheckoutScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('تم تأكيد الطلب بنجاح'),
      ),
    );
  }
}
